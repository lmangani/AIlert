# AIlert config example
# Usage: ailert run -config config.yaml

# Optional: persist pattern store (load/save on run)
store_path: ".ailert/store.json"

# Optional: emit alerts and create silences
# alertmanager_url: "http://localhost:9093"

# Optional: directory for snapshots (used by detect-changes, suggest-rules)
# snapshot_dir: ".ailert/snapshots"

sources:
  - id: app-log
    type: file
    path: /var/log/app.log
  # - id: metrics
  #   type: prometheus
  #   url: http://localhost:9090/metrics
  # - id: log-url
  #   type: http
  #   url: https://example.com/logs.txt
